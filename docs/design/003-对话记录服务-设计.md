# 003-对话记录服务-设计.md

## 变更记录表

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| 1.0 | 2026-03-02 | 初始版本 | AI |

---

## 1. 设计目标

### 1.1 要解决的问题

当前 `agent/hooks/observers/sqlite.go` 中直接使用 `database/sql` 操作 SQLite 数据库，存在以下问题：

1. **SQL 分散**：SQL 语句散落在 Observer 中，不利于维护
2. **重复劳动**：其他模块需要访问对话数据时，需要重复编写 SQL
3. **数据结构不一致**：不同模块可能使用不同的数据结构解析结果
4. **难以扩展**：更换数据库或修改表结构需要修改多处代码

### 1.2 功能目标

创建统一的对话记录 Service 层，提供：

1. **统一数据访问封装**：封装所有 CRUD 操作
2. **按对话ID查询单条原始对话**
3. **按会话ID/用户ID查询对话列表**
4. **按时间范围查询对话记录**
5. **分页查询对话列表**
6. **新增原始对话记录**
7. **屏蔽底层数据库细节**
8. **统一返回数据结构**

---

## 2. 核心设计思路

### 2.1 架构设计

```
┌─────────────────────────────────────────────────────────┐
│                    上层模块                               │
│  (Observer, API Handler, Analytics, etc.)                │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              ConversationService (统一接口)              │
│  - GetByConversationID                                   │
│  - ListBySessionKey                                      │
│  - ListByTimeRange                                       │
│  - ListWithPagination                                    │
│  - Create                                                │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              Repository Layer (数据访问层)                │
│              EventRepository                             │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              Database (SQLite)                           │
│              events 表                                    │
└─────────────────────────────────────────────────────────┘
```

### 2.2 模块职责划分

| 模块 | 职责 | 依赖 |
|------|------|------|
| `ConversationService` | 业务逻辑、数据转换、统一接口 | Repository |
| `EventRepository` | 数据库操作封装 | GORM/SQL |
| `EventModel` | 数据模型定义 | - |

### 2.3 数据流设计

```
┌──────────────┐
│   User/API   │
└──────┬───────┘
       │ 调用 Service
       ▼
┌──────────────────┐
│ ConversationSvc  │
│ (业务层)          │
└──────┬───────────┘
       │ 调用 Repository
       ▼
┌──────────────────┐
│ EventRepository  │
│ (数据访问层)      │
└──────┬───────────┘
       │ 执行 GORM
       ▼
┌──────────────────┐
│   SQLite DB      │
└──────────────────┘
```

---

## 3. 详细设计

### 3.1 数据模型设计

#### 3.1.1 Event 模型

```go
// Event 对应 events 表的数据模型
type Event struct {
    ID               uint      `gorm:"primarykey" json:"id"`
    TraceID          string    `gorm:"type:text;index;not null" json:"trace_id"`
    SpanID           string    `gorm:"type:text" json:"span_id"`
    ParentSpanID     string    `gorm:"type:text" json:"parent_span_id"`
    EventType        string    `gorm:"type:text;index;not null" json:"event_type"`
    Timestamp        time.Time `gorm:"type:datetime;index;not null" json:"timestamp"`
    SessionKey       string    `gorm:"type:text;index" json:"session_key"`
    Role             string    `gorm:"type:text;index" json:"role"`
    Content          string    `gorm:"type:text" json:"content"`
    PromptTokens     int       `gorm:"type:integer;default:0" json:"prompt_tokens"`
    CompletionTokens int       `gorm:"type:integer;default:0" json:"completion_tokens"`
    TotalTokens      int       `gorm:"type:integer;default:0" json:"total_tokens"`
    ReasoningTokens  int       `gorm:"type:integer;default:0" json:"reasoning_tokens"`
    CachedTokens     int       `gorm:"type:integer;default:0" json:"cached_tokens"`
    CreatedAt        time.Time `gorm:"type:datetime;default:CURRENT_TIMESTAMP" json:"created_at"`

    // TableName 指定表名
    func (Event) TableName() string {
        return "events"
    }
}
```

#### 3.1.2 DTO 设计

```go
// ConversationDTO 对话数据传输对象
type ConversationDTO struct {
    ID               uint      `json:"id"`
    TraceID          string    `json:"trace_id"`
    SpanID           string    `json:"span_id,omitempty"`
    ParentSpanID     string    `json:"parent_span_id,omitempty"`
    EventType        string    `json:"event_type"`
    Timestamp        time.Time `json:"timestamp"`
    SessionKey       string    `json:"session_key"`
    Role             string    `json:"role"`
    Content          string    `json:"content"`
    TokenUsage       *TokenUsageDTO `json:"token_usage,omitempty"`
    CreatedAt        time.Time `json:"created_at"`
}

// TokenUsageDTO Token 使用信息
type TokenUsageDTO struct {
    PromptTokens     int `json:"prompt_tokens"`
    CompletionTokens int `json:"completion_tokens"`
    TotalTokens      int `json:"total_tokens"`
    ReasoningTokens  int `json:"reasoning_tokens,omitempty"`
    CachedTokens     int `json:"cached_tokens,omitempty"`
}

// ConversationListResult 对话列表查询结果
type ConversationListResult struct {
    Conversations []ConversationDTO `json:"conversations"`
    Total         int64            `json:"total"`
    Page          int              `json:"page"`
    PageSize      int              `json:"page_size"`
}

// QueryOptions 查询选项
type QueryOptions struct {
    OrderBy    string // 排序字段
    Order      string // ASC 或 DESC
    Limit      int    // 限制数量
    Offset     int    // 偏移量
    Roles      []string // 筛选角色
}
```

### 3.2 Repository 层设计

```go
// EventRepository 事件仓储接口
type EventRepository interface {
    // FindByID 根据 ID 查找事件
    FindByID(ctx context.Context, id uint) (*Event, error)

    // FindByTraceID 根据 TraceID 查找事件
    FindByTraceID(ctx context.Context, traceID string) ([]Event, error)

    // FindBySessionKey 根据 SessionKey 查找事件
    FindBySessionKey(ctx context.Context, sessionKey string, opts *QueryOptions) ([]Event, error)

    // FindByTimeRange 根据时间范围查找事件
    FindByTimeRange(ctx context.Context, startTime, endTime time.Time, opts *QueryOptions) ([]Event, error)

    // CountBySessionKey 统计 SessionKey 下的事件数量
    CountBySessionKey(ctx context.Context, sessionKey string) (int64, error)

    // Create 创建事件
    Create(ctx context.Context, event *Event) error

    // CreateBatch 批量创建事件
    CreateBatch(ctx context.Context, events []Event) error

    // Count 统计事件总数
    Count(ctx context.Context) (int64, error)
}
```

### 3.3 Service 层设计

```go
// ConversationService 对话服务接口
type ConversationService interface {
    // GetByTraceID 根据 TraceID 获取完整对话
    GetByTraceID(ctx context.Context, traceID string) ([]ConversationDTO, error)

    // ListBySessionKey 根据会话 ID 获取对话列表（支持分页）
    ListBySessionKey(ctx context.Context, sessionKey string, page, pageSize int) (*ConversationListResult, error)

    // ListByTimeRange 根据时间范围获取对话列表（支持分页）
    ListByTimeRange(ctx context.Context, startTime, endTime time.Time, page, pageSize int) (*ConversationListResult, error)

    // ListRecent 获取最近的对话列表（支持分页）
    ListRecent(ctx context.Context, page, pageSize int) (*ConversationListResult, error)

    // Create 创建新对话记录
    Create(ctx context.Context, dto *ConversationDTO) error

    // CreateBatch 批量创建对话记录
    CreateBatch(ctx context.Context, dtos []ConversationDTO) error
}
```

### 3.4 错误处理设计

```go
var (
    // ErrRecordNotFound 记录不存在
    ErrRecordNotFound = errors.New("record not found")

    // ErrInvalidParameter 无效参数
    ErrInvalidParameter = errors.New("invalid parameter")

    // ErrDatabaseOperation 数据库操作错误
    ErrDatabaseOperation = errors.New("database operation failed")
)
```

---

## 4. 可能影响的模块

### 4.1 需要修改的模块

| 模块 | 修改内容 |
|------|----------|
| `agent/hooks/observers/sqlite.go` | 改用 Service 层接口，移除直接 SQL 操作 |

### 4.2 新增模块

| 模块 | 路径 | 职责 |
|------|------|------|
| Event Model | `agent/models/event.go` | 数据模型定义 |
| EventRepository | `agent/repository/event_repository.go` | 数据访问层 |
| ConversationService | `agent/service/conversation_service.go` | 业务服务层 |

### 4.3 模块依赖关系

```
ConversationService
    └──> EventRepository
           └──> GORM (数据库操作)

SQLiteObserver (修改)
    └──> ConversationService (新增)
```

---

## 5. 技术决策

### 5.1 为什么引入 GORM？

当前代码使用 `database/sql` 直接操作，但引入 Service 层后建议使用 GORM：

1. **减少 SQL 编写**：GORM 提供链式 API，代码更简洁
2. **类型安全**：编译时检查，减少运行时错误
3. **自动迁移**：支持表结构自动同步
4. **行业标准**：Go 社区广泛使用的 ORM，符合 `Go-GORM使用规范.md`

### 5.2 Repository 模式的作用

1. **解耦**：Service 层不依赖具体数据库实现
2. **测试友好**：可轻松 mock Repository 进行单元测试
3. **可替换**：未来可替换为其他数据库（MySQL、PostgreSQL）

### 5.3 分层设计的考虑

```
Controller/API (HTTP Handler)
         │
         ▼
    Service (业务逻辑)
         │
         ▼
  Repository (数据访问)
         │
         ▼
    Database (数据存储)
```

每层职责单一，符合单一职责原则（SRP）。

---

## 6. 安全考虑

### 6.1 SQL 注入防护

- GORM 使用参数化查询，自动防护 SQL 注入
- 禁止使用字符串拼接构建查询

### 6.2 敏感数据处理

- 使用 `json:"-"` 标签保护敏感字段（如 API Key、Token）
- 日志输出时脱敏处理

### 6.3 权限控制

- 所有查询操作传入 context，支持传递用户信息进行权限校验
- 分页查询限制最大返回数量（如 max 1000 条）

---

## 7. 性能考虑

### 7.1 索引优化

当前表已有索引：
- `idx_events_event_type`
- `idx_events_session_key`
- `idx_events_timestamp`
- `idx_events_trace_id`
- `idx_events_role`

新增建议索引：
- 复合索引：`(session_key, timestamp)` 用于会话时间线查询

### 7.2 查询优化

- 分页查询使用 `LIMIT` + `OFFSET`
- 大数据量查询考虑使用游标分页
- 批量插入使用 `CreateInBatches`

---

## 8. 测试策略

### 8.1 单元测试

- Repository 层：使用内存 SQLite 数据库
- Service 层：Mock Repository

### 8.2 集成测试

- 完整流程测试：Service → Repository → Database

### 8.3 测试覆盖

- 覆盖所有 CRUD 操作
- 覆盖边界条件（空值、超长字符串等）
- 覆盖错误场景

---

## 9. 实施计划

1. **阶段一**：创建数据模型和 Repository
2. **阶段二**：实现 Service 层
3. **阶段三**：编写单元测试
4. **阶段四**：重构 SQLiteObserver 使用新接口
5. **阶段五**：集成测试和文档更新

---

## 10. 后续扩展点

1. **缓存层**：引入 Redis 缓存热点数据
2. **全文搜索**：集成全文搜索引擎搜索对话内容
3. **数据归档**：定期归档历史对话数据
4. **多租户**：支持多租户隔离
5. **消息队列**：异步写入提升性能