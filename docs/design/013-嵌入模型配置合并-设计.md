# 013-嵌入模型配置合并到 LLMProvider - 设计文档

## 1. 架构概览

### 1.1 当前架构

```
┌─────────────────────────────────────────────────────────────┐
│                       配置来源                                │
├──────────────────────┬──────────────────────────────────────┤
│  LLMProvider 表      │  config.yaml / .env                  │
│  - 对话模型配置      │  - EmbeddingConfig                   │
│  - API Key/BaseURL   │    - APIKey/BaseURL                  │
└──────────────────────┴──────────────────────────────────────┘
           │                         │
           └───────────┬─────────────┘
                       │
              ┌────────▼────────┐
              │  向量化功能      │
              │  (Memory)        │
              └─────────────────┘
```

### 1.2 目标架构

```
┌─────────────────────────────────────────────────────────────┐
│                     统一配置来源                             │
│                   LLMProvider 表                            │
│  ┌────────────────────┬──────────────────────────────┐    │
│  │  对话模型配置      │  嵌入模型配置                 │    │
│  │  - SupportedModels │  - EmbeddingModels           │    │
│  │  - DefaultModel    │  - DefaultEmbeddingModel     │    │
│  │  - APIKey/BaseURL  │  (共享 APIKey/BaseURL)       │    │
│  └────────────────────┴──────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                           │
              ┌────────────▼────────────┐
              │   EmbeddingConfigService │
              │   - GetEmbeddingConfig   │
              │   - GetDefaultModel      │
              └────────────┬─────────────┘
                           │
              ┌────────────▼────────────┐
              │      向量化功能          │
              │      (Memory)            │
              └─────────────────────────┘
```

## 2. 数据模型设计

### 2.1 LLMProvider 模型扩展

```go
// internal/models/llm_provider.go

type LLMProvider struct {
    // ... 已有字段 ...

    // 嵌入模型配置（新增）
    EmbeddingModels       string `gorm:"type:text" json:"embedding_models,omitempty"`       // JSON数组
    DefaultEmbeddingModel string `gorm:"type:text" json:"default_embedding_model,omitempty"` // 默认模型ID
}

// EmbeddingModelInfo 嵌入模型信息
type EmbeddingModelInfo struct {
    ID         string `json:"id"`         // 模型ID
    Name       string `json:"name"`       // 显示名称
    Dimensions int    `json:"dimensions"` // 向量维度
}

// GetEmbeddingModels 获取嵌入模型列表
func (p *LLMProvider) GetEmbeddingModels() []EmbeddingModelInfo {
    if p.EmbeddingModels == "" || p.EmbeddingModels == "null" {
        return nil
    }
    var models []EmbeddingModelInfo
    if err := json.Unmarshal([]byte(p.EmbeddingModels), &models); err != nil {
        return nil
    }
    return models
}

// SetEmbeddingModels 设置嵌入模型列表
func (p *LLMProvider) SetEmbeddingModels(models []EmbeddingModelInfo) error {
    data, err := json.Marshal(models)
    if err != nil {
        return err
    }
    p.EmbeddingModels = string(data)
    return nil
}

// GetEmbeddingConfig 获取指定嵌入模型的完整配置
func (p *LLMProvider) GetEmbeddingConfig(modelID string) (*EmbeddingModelConfig, error) {
    models := p.GetEmbeddingModels()
    if len(models) == 0 {
        return nil, fmt.Errorf("no embedding models configured")
    }

    // 查找指定模型
    for _, m := range models {
        if m.ID == modelID {
            return &EmbeddingModelConfig{
                APIKey:     p.APIKey,
                BaseURL:    p.APIBase,
                Model:      m.ID,
                Dimensions: m.Dimensions,
            }, nil
        }
    }

    return nil, fmt.Errorf("embedding model %s not found", modelID)
}

// GetDefaultEmbeddingConfig 获取默认嵌入模型配置
func (p *LLMProvider) GetDefaultEmbeddingConfig() (*EmbeddingModelConfig, error) {
    modelID := p.DefaultEmbeddingModel
    if modelID == "" {
        // 使用第一个嵌入模型作为默认
        models := p.GetEmbeddingModels()
        if len(models) == 0 {
            return nil, fmt.Errorf("no embedding models configured")
        }
        modelID = models[0].ID
    }
    return p.GetEmbeddingConfig(modelID)
}

type EmbeddingModelConfig struct {
    APIKey     string
    BaseURL    string
    Model      string
    Dimensions int
}
```

### 2.2 配置结构简化

```go
// config/schema.go

// EmbeddingConfig 向量化配置（简化后）
type EmbeddingConfig struct {
    Enabled    bool   `json:"enabled"`    // 是否启用向量化
    Model      string `json:"model"`      // 默认模型（可选，留空则从数据库读取）
    Dimensions int    `json:"dimensions"` // 维度（可选，从数据库读取）
    // 移除: APIKey, BaseURL
}
```

## 3. 服务层设计

### 3.1 EmbeddingConfigService 接口

```go
// internal/service/embedding/config.go

package embedding

import (
    "context"
    "github.com/weibaohui/nanobot-go/internal/models"
    "gorm.io/gorm"
)

// Config 嵌入模型完整配置
type Config struct {
    ProviderID  uint   // Provider ID
    APIKey      string // API密钥
    BaseURL     string // API基础地址
    Model       string // 模型ID
    ModelName   string // 模型名称
    Dimensions  int    // 向量维度
}

// Service 嵌入配置服务接口
type Service interface {
    // GetConfig 获取嵌入模型配置
    // 优先使用指定模型，否则使用默认模型
    GetConfig(ctx context.Context, modelID string) (*Config, error)

    // GetDefaultConfig 获取默认嵌入模型配置
    GetDefaultConfig(ctx context.Context) (*Config, error)

    // ListProviders 列出支持嵌入的 Provider 列表
    ListProviders(ctx context.Context) ([]*models.LLMProvider, error)

    // GetProviderByModel 根据模型ID获取 Provider
    GetProviderByModel(ctx context.Context, modelID string) (*models.LLMProvider, error)
}

// service 实现
type service struct {
    db *gorm.DB
}

// NewService 创建服务
func NewService(db *gorm.DB) Service {
    return &service{db: db}
}
```

### 3.2 核心方法实现

```go
// GetConfig 实现
func (s *service) GetConfig(ctx context.Context, modelID string) (*Config, error) {
    // 1. 查询支持嵌入模型的 Provider
    var providers []models.LLMProvider
    err := s.db.WithContext(ctx).
        Where("is_active = ? AND embedding_models IS NOT NULL AND embedding_models != ''", true).
        Find(&providers).Error
    if err != nil {
        return nil, err
    }

    // 2. 如果指定了模型ID，查找对应 Provider
    if modelID != "" {
        for _, p := range providers {
            if cfg, err := p.GetEmbeddingConfig(modelID); err == nil {
                return &Config{
                    ProviderID: p.ID,
                    APIKey:     cfg.APIKey,
                    BaseURL:    cfg.BaseURL,
                    Model:      cfg.Model,
                    Dimensions: cfg.Dimensions,
                }, nil
            }
        }
        return nil, fmt.Errorf("embedding model %s not found", modelID)
    }

    // 3. 使用默认模型（第一个 Provider 的默认模型）
    for _, p := range providers {
        if cfg, err := p.GetDefaultEmbeddingConfig(); err == nil {
            return &Config{
                ProviderID: p.ID,
                APIKey:     cfg.APIKey,
                BaseURL:    cfg.BaseURL,
                Model:      cfg.Model,
                Dimensions: cfg.Dimensions,
            }, nil
        }
    }

    return nil, fmt.Errorf("no embedding model configured")
}

// GetDefaultConfig 实现
func (s *service) GetDefaultConfig(ctx context.Context) (*Config, error) {
    return s.GetConfig(ctx, "")
}
```

## 4. 使用流程

### 4.1 初始化时使用

```go
// internal/app/memory.go

func InitMemory(cfg *config.Config, db *gorm.DB, logger *zap.Logger) *MemoryComponents {
    // ... 其他初始化 ...

    // 创建嵌入配置服务
    embeddingSvc := embedding.NewService(db)

    // 获取嵌入模型配置
    embeddingCfg, err := embeddingSvc.GetDefaultConfig(context.Background())
    if err != nil {
        logger.Warn("获取嵌入模型配置失败", zap.Error(err))
        // 可以回退到配置文件（向后兼容）
    }

    // 使用配置创建嵌入客户端
    // embeddingClient := NewEmbeddingClient(embeddingCfg)
}
```

### 4.2 运行时获取

```go
// 向量化时使用
func (s *MemoryService) Vectorize(ctx context.Context, text string) ([]float32, error) {
    // 获取当前配置
    cfg, err := s.embeddingSvc.GetDefaultConfig(ctx)
    if err != nil {
        return nil, err
    }

    // 调用嵌入 API
    // return s.embedder.Embed(ctx, text, cfg.Model, cfg.APIKey)
}
```

## 5. 数据库迁移

### 5.1 迁移脚本

```sql
-- scripts/migrate_embedding.sql

-- 步骤1: 新增字段
ALTER TABLE llm_providers
    ADD COLUMN embedding_models TEXT,
    ADD COLUMN default_embedding_model TEXT;

-- 步骤2: 为现有 Provider 添加默认嵌入模型（OpenAI）
UPDATE llm_providers
SET
    embedding_models = '[
        {"id":"text-embedding-3-small","name":"Text Embedding 3 Small","dimensions":1536},
        {"id":"text-embedding-3-large","name":"Text Embedding 3 Large","dimensions":3072}
    ]',
    default_embedding_model = 'text-embedding-3-small'
WHERE provider_key = 'openai';

-- 步骤3: 为 Anthropic 添加（如果支持）
UPDATE llm_providers
SET
    embedding_models = '[]',
    default_embedding_model = ''
WHERE provider_key = 'anthropic';

-- 步骤4: 创建索引（可选）
CREATE INDEX IF NOT EXISTS idx_llm_providers_embedding
    ON llm_providers(embedding_models)
    WHERE embedding_models IS NOT NULL;
```

## 6. API 设计

### 6.1 Provider 管理 API 扩展

```go
// internal/api/handler_provider.go

// UpdateEmbeddingModelsRequest 更新嵌入模型请求
type UpdateEmbeddingModelsRequest struct {
    EmbeddingModels       []models.EmbeddingModelInfo `json:"embedding_models"`
    DefaultEmbeddingModel string                      `json:"default_embedding_model"`
}

// updateEmbeddingModels 更新 Provider 的嵌入模型配置
func (h *Handler) updateEmbeddingModels(c *gin.Context) {
    id := parseID(c, "id")

    var req UpdateEmbeddingModelsRequest
    if err := c.ShouldBindJSON(&req); err != nil {
        c.JSON(400, gin.H{"error": "invalid request"})
        return
    }

    // 获取 Provider
    provider, err := h.providerService.Get(c, id)
    if err != nil {
        c.JSON(404, gin.H{"error": "provider not found"})
        return
    }

    // 设置嵌入模型
    if err := provider.SetEmbeddingModels(req.EmbeddingModels); err != nil {
        c.JSON(500, gin.H{"error": err.Error()})
        return
    }
    provider.DefaultEmbeddingModel = req.DefaultEmbeddingModel

    // 保存
    if err := h.providerService.Update(c, id, provider); err != nil {
        c.JSON(500, gin.H{"error": err.Error()})
        return
    }

    c.JSON(200, provider)
}
```

### 6.2 路由

```go
// internal/api/handler.go
providers := authorized.Group("/providers")
{
    // ... 原有路由 ...
    providers.PUT("/:id/embedding", h.updateEmbeddingModels)
}
```

## 7. 前端适配

### 7.1 类型定义

```typescript
// web/src/types/index.ts

export interface EmbeddingModelInfo {
  id: string;
  name: string;
  dimensions: number;
}

export interface LLMProvider {
  id: number;
  // ... 原有字段 ...
  embedding_models?: EmbeddingModelInfo[];
  default_embedding_model?: string;
}
```

### 7.2 Provider 编辑表单

在 Provider 管理页面添加嵌入模型配置表单：

```typescript
// 嵌入模型配置
<Form.Item label="嵌入模型">
  <Table
    dataSource={embeddingModels}
    columns={[
      { title: '模型ID', dataIndex: 'id' },
      { title: '名称', dataIndex: 'name' },
      { title: '维度', dataIndex: 'dimensions' },
      { title: '操作', render: (_, record) => (
        <Button onClick={() => removeModel(record.id)}>删除</Button>
      )}
    ]}
  />
  <Button onClick={addModel}>添加模型</Button>
</Form.Item>

<Form.Item label="默认嵌入模型">
  <Select
    value={defaultEmbeddingModel}
    options={embeddingModels.map(m => ({ label: m.name, value: m.id }))}
  />
</Form.Item>
```

## 8. 向后兼容策略

### 8.1 配置回退

```go
func (s *service) GetConfig(ctx context.Context, modelID string) (*Config, error) {
    // 1. 尝试从数据库获取
    cfg, err := s.getConfigFromDB(ctx, modelID)
    if err == nil {
        return cfg, nil
    }

    // 2. 如果数据库没有配置，尝试从配置文件读取（向后兼容）
    if s.fallbackConfig != nil && s.fallbackConfig.APIKey != "" {
        logger.Warn("使用配置文件中的嵌入模型配置（已弃用）")
        return &Config{
            APIKey:     s.fallbackConfig.APIKey,
            BaseURL:    s.fallbackConfig.BaseURL,
            Model:      s.fallbackConfig.Model,
            Dimensions: s.fallbackConfig.Dimensions,
        }, nil
    }

    return nil, fmt.Errorf("no embedding model configured")
}
```

### 8.2 迁移提示

在启动时检查并提示：

```go
if cfg.Embedding.APIKey != "" {
    logger.Warn("配置文件中的 Embedding APIKey 已弃用，请迁移到数据库配置")
}
```

## 9. 测试策略

### 9.1 单元测试

```go
func TestEmbeddingService_GetConfig(t *testing.T) {
    // 测试场景：
    // 1. 指定模型ID存在
    // 2. 指定模型ID不存在
    // 3. 使用默认模型
    // 4. 没有配置时回退到配置文件
}

func TestLLMProvider_GetEmbeddingConfig(t *testing.T) {
    // 测试场景：
    // 1. 获取指定模型配置
    // 2. 获取默认模型配置
    // 3. 模型不存在
}
```

### 9.2 集成测试

```go
func TestEmbeddingIntegration(t *testing.T) {
    // 测试完整流程：
    // 1. 创建 Provider 并配置嵌入模型
    // 2. 调用 GetDefaultConfig 获取配置
    // 3. 验证配置内容正确
}
```

## 10. 实施计划

| 阶段 | 内容 | 预计时间 |
|------|------|----------|
| 1 | 数据库模型扩展 | 1天 |
| 2 | EmbeddingConfigService 实现 | 1天 |
| 3 | 配置文件简化 + 向后兼容 | 1天 |
| 4 | API 扩展 | 0.5天 |
| 5 | 前端适配 | 1天 |
| 6 | 测试 + 文档 | 1天 |

## 11. 风险评估

| 风险 | 等级 | 缓解措施 |
|------|------|----------|
| 配置迁移失败 | 中 | 向后兼容机制，保留配置文件读取 |
| 嵌入模型配置错误 | 低 | 配置验证，维度检查 |
| 多 Provider 选择歧义 | 低 | 清晰的默认选择逻辑 |
